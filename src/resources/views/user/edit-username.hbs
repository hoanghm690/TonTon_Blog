{{#if user}}
<div class="container mt-5" style="min-height: 100vh">
    <div class="row">
        <div class="col-lg-4">
            <a href="/" class="d-flex" style="cursor: pointer; text-decoration: none; color: #333">
                <img class="rounded mr-2 mb-5" src="{{user.avatar}}" style="width: 69px; height: 69px">
                <h5>{{user.username}}</h5>
            </a>
        </div>

        <div class="col-lg-6">
            <a href="/user/settings/accounts" style="text-decoration: none">
                <i class="fas fa-chevron-left"></i>
                <span>Tài khoản</span>
            </a>
            <div class="d-flex mt-4 mb-4">
                <div>
                    <h2 class="mb-3" style="font-weight: 500; font-size: 2.4rem">Thông tin cá nhân</h2>
                    <p>Thông tin cá nhân của bạn là cơ sở để tạo hồ sơ xin việc trong tương lai. Thông tin cập nhật tại
                        đây sẽ được chúng tôi kiểm duyệt.</p>
                </div>
            </div>
            <form method="POST" action="/user/{{user._id}}?_method=PUT">
                <div class="form-group">
                    <h5 style="font-size: 1.1rem; font-weight: 500">Giới tính</h5>
                    <div class="d-flex">
                        <div class="mr-4">
                            <input id="male" name="gender" type="radio" value="male">
                            <label for="male">Nam</label>
                        </div>
                        <div>
                            <input id="female" name="gender" type="radio" value="female">
                            <label for="female">Nữ</label>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <label for="username" style="font-size: 1.1rem; font-weight: 500">Tên</label>
                    <input type="text" class="form-control" id="username" value="{{user.username}}" name="username"
                        style="color: black;">
                </div>
                <div class="form-group">
                    <label for="birthday" style="font-size: 1.1rem; font-weight: 500">Ngày sinh</label>
                    <input type="text" class="form-control" id="birthday" name="birthday"
                        placeholder="ngày/tháng/năm (vd: 06/09/2000)">
                </div>
                <button type="submit" class="btn btn-primary float-right mt-2">Cập nhật</button>
            </form>
            <div class="d-inline-flex mt-4 align-items-center" style="background-color: #eff9fe">
                <i class="fas fa-info-circle ml-3"></i>
                <p style="margin: 15px 15px 15px 15px">
                    Sử dụng chính xác thông tin trên căn cước công dân của bạn để thuận tiện cho việc tự động tạo thông
                    tin hồ sơ xin việc trong tương lai.
                </p>
            </div>
        </div>
    </div>
</div>
{{/if}}